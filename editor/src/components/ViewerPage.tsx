export function ViewerPage() {
  return (
    <main className="viewer-page">
      <section className="panel panel--hero">
        <p className="eyebrow">Viewer</p>
        <h2>Viewer page placeholder</h2>
        <p>
          The editor route is ready for building forms. The viewer route is intentionally left as a
          stub for the next step so we can keep the data model and routing aligned from the start.
        </p>
      </section>
    </main>
  );
}
